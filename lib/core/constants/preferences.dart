class Preferences {
  const Preferences._();

  // Secure Storage Keys
  static const String accessToken = 'access_token';
  static const String refreshToken = 'refresh_token';

  // Shared Preferences Keys
  static const String initialLaunch = 'initial_launch';
}
